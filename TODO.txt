# refactor:
  Clone::is_visible -> Node::is_visible
  Clone::setParent(Clone*) -> Node::setParent(Node*)

# avoid masked positions ('N', lower case letters?) from being mutated

# VCF output: flag private mutations
